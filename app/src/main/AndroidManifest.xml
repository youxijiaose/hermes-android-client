<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Local network for connecting to Termux -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- Voice input -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- File attachment -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- OTA Update -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />

    <application
        android:allowBackup="true"
        android:name=".util.HermesApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HermesClient.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- FileProvider for APK installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.HermesClient.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings"
            android:parentActivityName=".ui.MainActivity" />
            
        <activity
            android:name=".ui.ApprovalActivity"
            android:exported="false"
            android:label="@string/approval"
            android:theme="@style/Theme.HermesClient" />
            
        <activity
            android:name=".ui.SessionsActivity"
            android:exported="false"
            android:label="@string/sessions"
            android:parentActivityName=".ui.MainActivity" />
            
        <activity
            android:name=".ui.MemoryActivity"
            android:exported="false"
            android:label="Memory"
            android:parentActivityName=".ui.MainActivity" />
            
        <activity
            android:name=".ui.SkillsActivity"
            android:exported="false"
            android:label="Skills"
            android:parentActivityName=".ui.MainActivity" />
            
        <activity
            android:name=".ui.BrowserActivity"
            android:exported="false"
            android:label="Browser"
            android:parentActivityName=".ui.MainActivity" />
            
        <activity
            android:name=".ui.CronActivity"
            android:exported="false"
            android:label="Cron Jobs"
            android:parentActivityName=".ui.MainActivity" />
    </application>

</manifest>
